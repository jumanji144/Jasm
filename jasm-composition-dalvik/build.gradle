apply plugin: 'java-library'

repositories {
    mavenLocal()
    mavenCentral()
    maven { url = 'https://jitpack.io' }
}

dependencies {
    api 'me.darknet:dex-core:0.0.1-alpha'

    api project(path: ':jasm-core')
}

test {
    useJUnitPlatform()
}